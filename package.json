{
  "name": "nestjs-extract-eval",
  "version": "1.0.0",
  "description": "Minimal NestJS fixture for testing repository extraction pattern",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "echo 'lint ok'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests configured'"
  },
  "dependencies": {
    "@nestjs/common": "^10.3.0",
    "@nestjs/core": "^10.3.0",
    "@nestjs/typeorm": "^10.0.1",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^7.8.1",
    "typeorm": "^0.3.17"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
