/**
 * NotificationsService unit tests.
 */
describe('NotificationsService', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });

  it('should create a notification', () => {
    expect(true).toBe(true);
  });

  it('should find unread notifications for recipient', () => {
    expect(true).toBe(true);
  });

  it('should mark a single notification as read', () => {
    expect(true).toBe(true);
  });

  it('should mark all notifications as read for recipient', () => {
    expect(true).toBe(true);
  });

  it('should return unread count', () => {
    expect(true).toBe(true);
  });

  it('should delete old read notifications', () => {
    expect(true).toBe(true);
  });
});
